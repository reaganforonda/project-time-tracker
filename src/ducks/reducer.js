const INITIAL_STATE = {
    user : {}
};

const GET_USER_INFO = 'GET_USER_INFO';


export default function reducer(state = INITIAL_STATE, action){
    switch(action.type){
        // TODO:
        default:
        return state;
    }
}