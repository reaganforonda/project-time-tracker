const INITIAL_STATE = {
    // TODO:
};

// TODO:


export default function reducer(state = INITIAL_STATE, action){
    switch(action.type){
        // TODO:
        default:
        return state;
    }
}